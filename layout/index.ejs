<div class="topic_header">
    <span class="recent">最新博文</span>
</div>
<% page.posts.each(function(item){ %>
  <%- partial('_partial/index', {item: item, index: true}) %>
<% }); %>

<%- partial('_partial/pagination') %>
